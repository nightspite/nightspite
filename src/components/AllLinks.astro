---
import { SOCIALS } from "@config";
---

<div>
  {
    (
      <a
        href={SOCIALS.find(social => social.name === "Linkinbio")?.href || "/"}
        target="_blank"
        rel="noopener noreferrer"
        class="link"
      >
        All links
      </a>
    )
  }
</div>
